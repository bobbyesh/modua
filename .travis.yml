language: python
python:
  - "3.5"
install: "pip3 install -r requirements.txt"
script: 
  - python modua/manage.py test
services:
  - postgresql
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
